/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package bdd.automation.api.app;

import bdd.automation.api.list.LinkedList;

import static bdd.automation.api.utilities.StringUtils.join;
import static bdd.automation.api.utilities.StringUtils.split;
import static bdd.automation.api.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
